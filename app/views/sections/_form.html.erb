<%= render 'layouts/errors', :object => @section %>

<%= form_for @section do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :chapter_id %>
  <%= f.collection_select :chapter_id, Chapter.order(:name),:id,:name, include_blank: false %>
  <%= f.submit %>
<%end%>
